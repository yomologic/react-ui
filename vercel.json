{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "yarn build && cd site && yarn build",
    "installCommand": "node vercel-setup.js && cd site && yarn install",
    "outputDirectory": "site/.next"
}
