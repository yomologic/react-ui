{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "sed -i 's/\"private\": false/\"private\": true/' package.json && yarn install && yarn workspace @yomologic/react-ui-site build",
  "installCommand": "echo 'Handled in buildCommand'",
  "outputDirectory": "site/.next"
}
